= form_for(@question) do |f|
  h1  Title:
  p
    = f.text_field :title, placeholder: "New title"
  h1 Text:
  p 
    = f.text_area(:body, cols: 100, rows: 10)
  p 
    = f.submit "Post Your Question"
