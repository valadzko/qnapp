# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
# ready = ->
#   $('.add-comment-link').click (e) ->
#     e.preventDefault()
#     $(this).hide()
#     $('.add-comment-form').show()
#     return
#
#   # $('.add-comment-form').bind 'ajax:success', (e, data, status, xhr) ->
#   #   alert("Ajax was successful")
#   #   comment = $.parseJSON(xhr.responseText)
#   #   console.log(comment)
#   #   $('.add-comment-form').find("input#comment_content").val('')
#   #   $('.add-comment-link').show()
#   #   $('.question-comments').append(comment)
#   # .bind 'ajax:error', (e, xhr, status, error) ->
#   #   response = $.parseJSON(xhr.responseText)
#   #   $('.comment-errors').html(response.errors)
#
# $(document).ready(ready)
# $(document).on("page:load", ready)
# $(document).on("page:update", ready)
# $(document).on("turbolinks:load", ready)
